# LUNA
LUNA is a lightweight, high-performance C++ local user and network analyzer. Operating under the KISS principle, it monitors active network interfaces, maps local user traffic, and yields deep socket analytics directly in the console without relying on heavy external dependencies, optimized for rapid security auditing.
